{
  "name": "@ins/snap-resolver",
  "version": "0.1.0",
  "description": "MetaMask Snap — native .igra name resolution via INS (Igra Name Service)",
  "main": "dist/snap.js",
  "files": [
    "dist/",
    "snap.manifest.json",
    "images/"
  ],
  "scripts": {
    "build": "mm-snap build",
    "serve": "mm-snap serve",
    "watch": "mm-snap watch",
    "clean": "rimraf dist .cache"
  },
  "keywords": [
    "metamask",
    "snap",
    "ins",
    "igra",
    "kaspa",
    "name-service",
    "ens"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/ItsGoonBoyCrypto/INSdomains"
  },
  "dependencies": {
    "@metamask/snaps-sdk": "^6.10.0"
  },
  "devDependencies": {
    "@metamask/snaps-cli": "^6.5.0",
    "@types/node": "^20.10.0",
    "rimraf": "^5.0.5",
    "typescript": "^5.7.3"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
