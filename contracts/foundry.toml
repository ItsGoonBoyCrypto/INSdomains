[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc = "0.8.24"
optimizer = true
optimizer_runs = 2000
via_ir = true
bytecode_hash = "none"

[rpc_endpoints]
igra = "${IGRA_RPC}"

[etherscan]
igra = { key = "${IGRA_EXPLORER_KEY}", url = "https://explorer.igralabs.com/api" }
